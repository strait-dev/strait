package store

// testInternalSecret is the internal secret constant shared across store
// package tests. Defined once to avoid magic string duplication.
const testInternalSecret = "test-secret-value"
