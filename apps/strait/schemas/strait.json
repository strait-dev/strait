{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://api.strait.dev/schemas/v1/strait.json",
  "title": "Strait Configuration",
  "description": "Configuration file for Strait SDK projects. Place this file at the root of your project as strait.json. IDEs (VS Code, JetBrains) will auto-complete fields and validate values when the $schema field is present.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference. Set to https://api.strait.dev/schemas/v1/strait.json for IDE autocomplete."
    },
    "project": {
      "type": "object",
      "description": "Project identity. The id must match the project created in the Strait dashboard.",
      "additionalProperties": false,
      "required": ["id"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Project ID from the Strait dashboard (e.g. proj_abc123)."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable project name. Used in CLI output and dashboard labels."
        }
      }
    },
    "endpoint_url": {
      "type": "string",
      "format": "uri",
      "description": "The URL where the Strait platform sends job execution requests (webhook endpoint). This is what createServeHandler() listens on. Per-environment overrides go in the environments section."
    },
    "dirs": {
      "type": "array",
      "description": "Directories containing job, workflow, and worker definitions. Used by 'strait register' and local development tooling. Paths are relative to the strait.json location.",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1
    },
    "retries": {
      "type": "object",
      "description": "Default retry policy applied to all jobs. Can be overridden per-job in the SDK.",
      "additionalProperties": false,
      "properties": {
        "max_attempts": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "Maximum number of retry attempts after the initial failure. 0 means no retries."
        },
        "strategy": {
          "type": "string",
          "enum": ["exponential", "fixed"],
          "description": "Backoff strategy between retry attempts."
        },
        "initial_delay_secs": {
          "type": "number",
          "minimum": 0,
          "description": "Delay before the first retry in seconds."
        },
        "max_delay_secs": {
          "type": "number",
          "minimum": 0,
          "description": "Maximum delay between retries in seconds. Only applies to exponential and linear strategies."
        }
      }
    },
    "max_duration_secs": {
      "type": "integer",
      "minimum": 1,
      "maximum": 86400,
      "description": "Default maximum execution time in seconds for all jobs. Overridable per-job. Maximum is 86400 (24 hours)."
    },
    "sdk": {
      "type": "object",
      "description": "SDK connection settings. Auth tokens are never stored here — use environment variables.",
      "additionalProperties": false,
      "properties": {
        "base_url": {
          "type": "string",
          "format": "uri",
          "description": "Strait API base URL. Defaults to https://api.strait.dev."
        },
        "auth_type": {
          "type": "string",
          "enum": ["apiKey", "bearer"],
          "description": "Authentication method used by the SDK."
        },
        "timeout_ms": {
          "type": "integer",
          "minimum": 0,
          "description": "HTTP request timeout in milliseconds for SDK API calls."
        },
        "debug": {
          "type": "boolean",
          "description": "Enable verbose SDK logging. Do not enable in production."
        },
        "api_key_env": {
          "type": "string",
          "minLength": 1,
          "description": "Name of the environment variable holding the default API key (e.g. STRAIT_API_KEY). The key itself is never stored in this file."
        }
      }
    },
    "deploy": {
      "type": "object",
      "description": "Optional deployment metadata used by CLI workflows. Strait does not build images or run job code; deployments describe customer-owned releases.",
      "additionalProperties": false,
      "properties": {
        "environment": {
          "type": "string",
          "minLength": 1,
          "description": "Default target environment for release metadata."
        },
        "artifact_uri": {
          "type": "string",
          "description": "Optional URI for a customer-owned release artifact, image, or bundle. Strait stores this as metadata only."
        },
        "manifest_path": {
          "type": "string",
          "description": "Optional path to release metadata generated by your own build pipeline."
        }
      }
    },
    "worker": {
      "type": "object",
      "description": "Worker process settings. Workers connect to Strait via gRPC streaming and execute jobs locally.",
      "additionalProperties": false,
      "properties": {
        "concurrency": {
          "type": "integer",
          "minimum": 1,
          "maximum": 1000,
          "description": "Number of job runs the worker executes concurrently."
        },
        "grpc_url": {
          "type": "string",
          "format": "uri",
          "description": "gRPC server URL the worker connects to (e.g. grpc://api.strait.dev:50051 or grpcs://api.strait.dev:50051 for TLS). Defaults to the SDK base_url on port 50051."
        },
        "queue_name": {
          "type": "string",
          "minLength": 1,
          "description": "Named queue this worker drains. Workers without a queue_name drain the default queue for the project."
        },
        "signing_secret_env": {
          "type": "string",
          "minLength": 1,
          "description": "Name of the environment variable holding the HMAC signing secret used to verify job endpoint callbacks. The secret itself is never stored in this file."
        }
      }
    },
    "environments": {
      "type": "object",
      "description": "Per-environment configuration overrides. Select an environment with 'strait --env <name>' or via the CLI default_environment setting.",
      "additionalProperties": {
        "$ref": "#/$defs/environment"
      }
    },
    "cli": {
      "type": "object",
      "description": "CLI preferences.",
      "additionalProperties": false,
      "properties": {
        "default_environment": {
          "type": "string",
          "description": "Environment name to use when --env is not specified."
        },
        "output_format": {
          "type": "string",
          "enum": ["table", "json", "yaml"],
          "description": "Default output format for CLI commands."
        }
      }
    }
  },
  "$defs": {
    "environment": {
      "type": "object",
      "description": "Environment-specific overrides. Any field here replaces the top-level value for that environment.",
      "additionalProperties": false,
      "properties": {
        "base_url": {
          "type": "string",
          "format": "uri",
          "description": "Strait API base URL for this environment."
        },
        "endpoint_url": {
          "type": "string",
          "format": "uri",
          "description": "Webhook endpoint URL for this environment."
        },
        "api_key_env": {
          "type": "string",
          "minLength": 1,
          "description": "Name of the environment variable holding the API key for this environment (e.g. STRAIT_STAGING_API_KEY)."
        }
      }
    }
  }
}
