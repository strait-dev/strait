# Dedicated ServiceAccount for Strait job pods.
# No RBAC bindings -- job pods should have zero K8s API access.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: strait-job-runner
  namespace: default
  labels:
    app: strait
automountServiceAccountToken: false
