/**
 * Default length of organization slug in the UI.
 */
export const ORGANIZATION_SLUG_LENGTH = 8;

/**
 * Maximum pincode length
 */
export const MAX_PINCODE_LENGTH = 30;

/**
 * Milliseconds in a second (for time conversions)
 */
export const MILLISECONDS_PER_SECOND = 1000;

/**
 * Timer interval for countdown in milliseconds
 */
export const TIMER_INTERVAL_MS = 1000;

/**
 * Percentage conversion factor (to convert decimal to percentage)
 */
export const PERCENTAGE_MULTIPLIER = 100;

/**
 * Maximum slug length for organization names
 */
export const MAX_SLUG_LENGTH = 50;

/**
 * Default pincode length
 */
export const DEFAULT_PINCODE_LENGTH = 6;

