---
import Shell from "@/components/layout/shell.tsx";

const PAIN_POINTS = [
  {
    before: "4 queue systems that disagree on retries",
    after: "One queue with built-in retry strategies",
  },
  {
    before: "Shell scripts coordinating job dependencies",
    after: "Workflow DAGs with conditions and fan-out",
  },
  {
    before: "30 minutes joining logs across 3 services",
    after: "Per-run event timeline with debug bundles",
  },
  {
    before: "Every team reinventing job infrastructure",
    after: "Shared platform with 5 language SDKs",
  },
];
---

<section
  aria-label="The problem with background job systems"
  class="bg-muted/30 py-16 sm:py-20"
>
  <Shell variant="wide">
    <div class="mb-14 max-w-3xl">
      <h2 class="text-balance text-2xl leading-[1.2] sm:text-3xl lg:text-4xl">
        Stop duct-taping your job infrastructure.
      </h2>
      <p class="mt-3 text-pretty text-muted-foreground text-sm leading-relaxed sm:text-base">
        Every team builds the same glue code -- retries, queues, monitoring.
        Strait replaces all of it.
      </p>
    </div>

    <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
      {PAIN_POINTS.map((point) => (
        <div class="rounded-xl border border-border/40 bg-card p-5 sm:p-6">
          <p class="text-muted-foreground/50 text-sm leading-relaxed line-through decoration-destructive/40">
            {point.before}
          </p>
          <p class="mt-2 font-medium text-foreground text-sm leading-relaxed">
            <span class="mr-2 inline-block size-1.5 rounded-full bg-primary" />
            {point.after}
          </p>
        </div>
      ))}
    </div>
  </Shell>
</section>
