---
title: Event Triggers
description: External event trigger management for durable waits
full: true
_openapi:
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Returns a paginated list of event triggers, optionally filtered by
          status, workflow run, or source type.
      - content: >-
          Returns aggregate counts by status and average wait duration for the
          project.
      - content: Returns a single event trigger by its globally unique event key.
      - content: >
          Cancels a waiting event trigger by key. The associated workflow step
          is failed

          (respecting on_failure policy) or the job run is canceled.
      - content: |
          Opens a Server-Sent Events stream for a specific event trigger.
          Sends the current state immediately, then streams status changes
          until the trigger reaches a terminal state.
      - content: >
          Delivers an event payload to a waiting trigger. The trigger
          transitions to 'received'

          and the associated workflow step is completed or job run is re-queued.


          Event keys are globally unique. Recommended patterns:

          - Workflow steps: `{domain}:{entity_id}` — e.g., `aml-check:app-456`

          - SDK runs: `{service}:{correlation_id}` — e.g., `payment:order-789`

          - Per-execution uniqueness: include `{{workflow_run_id}}` in the
          template
      - content: >
          Delivers an event payload to all waiting triggers whose event_key
          starts

          with the given prefix. Each matched trigger is resolved individually.

          Results are scoped to the authenticated project when using API key
          auth.

          Returns up to 1000 matching triggers per request.
      - content: >
          Deletes event triggers in terminal state (received, timed_out,
          canceled)

          older than the specified number of days. Use dry_run to preview.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

<APIPage document={"openapi"} webhooks={[]} operations={[{"path":"/v1/events","method":"get"},{"path":"/v1/events/stats","method":"get"},{"path":"/v1/events/{eventKey}","method":"get"},{"path":"/v1/events/{eventKey}","method":"delete"},{"path":"/v1/events/{eventKey}/stream","method":"get"},{"path":"/v1/events/{eventKey}/send","method":"post"},{"path":"/v1/events/prefix/{prefix}/send","method":"post"},{"path":"/v1/events/purge","method":"post"}]} showTitle={true} />