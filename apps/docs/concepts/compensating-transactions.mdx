---
title: "Compensating Transactions"
description: "Undo completed workflow steps when a later step fails."
sidebarTitle: "Compensation"
---

## Common Tasks

- [Build multi-step workflows](/use-cases/multi-step-workflows)
- [Troubleshoot failed runs](/guides/troubleshooting-run-failed)
- [Inspect runs and logs](/guides/inspect-runs)

When a later workflow step fails, earlier steps may need to be undone.

Compensating transactions let you attach an undo job to a step. When a workflow run ends up failed or timed out, you compensate it, and Strait runs the undo jobs for the completed steps in reverse order.

## When to Use Compensation

Use compensation when workflow steps have side effects that may need to be reversed:

- **Payments**: Charge a card, then something fails downstream. Refund the charge.
- **Reservations**: Reserve inventory or capacity, then a later step fails. Release the reservation.
- **External API calls**: Create a resource in a third-party system, then delete it if a later step fails.

If a step is read-only, it usually does not need a compensation job.

## Example: Order Fulfillment Saga

```json
{
  "steps": [
    {
      "step_ref": "create-order",
      "job_id": "order-create",
      "compensation_job_id": "order-cancel",
      "compensation_timeout_secs": 30
    },
    {
      "step_ref": "charge-card",
      "job_id": "payment-charge",
      "depends_on": ["create-order"],
      "compensation_job_id": "payment-refund",
      "compensation_timeout_secs": 30
    },
    {
      "step_ref": "reserve-inventory",
      "job_id": "inventory-reserve",
      "depends_on": ["charge-card"],
      "compensation_job_id": "inventory-release",
      "compensation_timeout_secs": 30
    },
    {
      "step_ref": "send-confirmation",
      "job_id": "notifications-send",
      "depends_on": ["reserve-inventory"]
    }
  ]
}
```

If `reserve-inventory` fails after all retries, the run lands in `failed`. Compensating it makes Strait:

1. Run `payment-refund` (with the output from `charge-card` as input)
2. Run `order-cancel` (with the output from `create-order` as input)

Only completed steps with a `compensation_job_id` are undone. `send-confirmation` has no compensation job, so Strait skips it.

## Triggering Compensation

Compensation is an explicit recovery action on a `failed` or `timed_out` run, not an automatic rollback at the moment a step fails. Trigger it with:

```http
POST /v1/workflow-runs/{workflowRunID}/compensate
```

Preview which steps would be undone, and in what order, before committing:

```http
GET /v1/workflow-runs/{workflowRunID}/compensation-plan
```

Once triggered:

1. Strait builds the plan from completed steps that have a `compensation_job_id`, in reverse execution order.
2. Each compensation job receives the original step output as input.
3. The run moves to `compensating`, then to a terminal compensation state.

## Workflow Run States

| State | Meaning |
| :--- | :--- |
| `compensating` | Compensation jobs are running. |
| `compensated` | All compensation jobs completed. Terminal. |
| `compensation_failed` | At least one compensation job failed. Terminal. Requires manual intervention. |

Common flow: `failed` or `timed_out` -> `compensating` -> `compensated` or `compensation_failed`.

<Warning>
Compensation jobs should be idempotent. If a compensation job fails, the workflow moves to `compensation_failed` and needs review.
</Warning>

## Configuration Reference

| Field | On | Description |
| :--- | :--- | :--- |
| `compensation_job_id` | Step | The job to run as the undo action for this step. |
| `compensation_timeout_secs` | Step | Timeout for the compensation job. |

A step's `on_failure` policy (`fail_workflow`, `skip_dependents`, or `continue`) decides whether a failed step fails the whole run. Compensation applies to a run that has already reached `failed` or `timed_out`, so leave failing steps on the default `fail_workflow` if you intend to compensate them.

## Related

- [Workflows](/concepts/workflows)
- [Durable Workflows](/concepts/durable-workflows)
- [Handle Failed Runs](/guides/handle-failed-runs)
- [Retry Strategies](/concepts/retry-strategies)
