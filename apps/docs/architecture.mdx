---
title: "Architecture"
description: "How Strait fits into your application."
icon: "sitemap"
og:image: "/images/og.png"
---

Strait coordinates background jobs and workflows. Your application still owns the business logic. Strait handles when work runs, how it is retried, and how each run is tracked.

## The Model

```text
Your app or service
        |
        | trigger a job or workflow
        v
Strait API
        |
        | stores state and queues work
        v
Strait worker
        |
        | calls your HTTP endpoint or connected worker
        v
Your code
```

Strait does not run customer code inside the Strait process. It dispatches work to code you own, then records the result so you can monitor, retry, replay, and audit it.

## What Runs

Strait ships as one Go binary with three modes:

| Mode | Use it for |
|---|---|
| `all` | Local development and smaller deployments. Runs the API, worker, and scheduler together. |
| `api` | Serving API traffic behind a load balancer. |
| `worker` | Processing queued jobs and workflow steps. |

You can start with `all` and split API and worker processes later as traffic grows.

## Required Services

| Service | Purpose |
|---|---|
| PostgreSQL | Stores projects, jobs, runs, workflows, and queued work. |
| Redis | Powers live updates and pub/sub notifications. |

Self-hosted Docker Compose includes the supporting services Strait needs. For setup instructions, see [Quick Start](/quickstart) and [Deployment](/guides/deployment).

## Execution Modes

Strait supports two ways to run customer code:

- **HTTP jobs** -- Strait sends a POST request to your configured endpoint.
- **Worker jobs** -- your worker keeps a connection open to Strait and receives work over that connection.

Use HTTP jobs when your code is already exposed as an endpoint. Use worker jobs when the code should stay inside your private network.

## Runs and Workflows

A job trigger creates a run. Most runs move from `queued` to `executing` to `completed`. Failed runs retry according to the job's retry policy. If retries are exhausted, the run moves to `dead_letter` so it can be inspected and replayed.

Workflows connect jobs into ordered steps. Steps can run in parallel, wait for approvals or external events, pass data between steps, and recover failed branches.

## Monitoring

Strait records run status, attempts, timing, logs, and workflow progress. You can watch runs in real time, subscribe to webhooks, export metrics, and inspect failures through the dashboard or API.

## Read Next

- [Jobs](/concepts/jobs) -- define work Strait can trigger.
- [Runs](/concepts/runs) -- track execution and retries.
- [Workflows](/concepts/workflows) -- coordinate multi-step processes.
- [Execution Modes](/concepts/execution-modes) -- choose HTTP or connected workers.
- [Deployment](/guides/deployment) -- run Strait in production.
