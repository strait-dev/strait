FROM python:3.12-slim

RUN pip install --no-cache-dir requests

COPY python/ /app/

WORKDIR /app

# Set SCRIPT_NAME env var to choose which script to run.
# Example: SCRIPT_NAME=fast_processor.py
ENTRYPOINT ["sh", "-c", "python /app/${SCRIPT_NAME}"]
