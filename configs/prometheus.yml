# Prometheus configuration for Strait self-hosted monitoring.
# Scrapes the Strait API's /metrics endpoint for Prometheus metrics.

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "strait"
    static_configs:
      - targets: ["strait:8080"]
    metrics_path: /metrics
